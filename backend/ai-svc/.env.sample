# AI Service
AI_SVC_PORT=3009

# Database (Supabase) — 공유 DB, 각 서비스별 독립 커넥션 풀 사용
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres

# ML Server — 실제 ML 서버가 없거나 연결에 실패하면 더미 결과로 대체 동작(데모/로컬 개발용)
# 로컬 개발: http://localhost:8001
# 프로덕션 (docker-compose 내부 네트워크): http://ml-server:8001
ML_SERVER_URL=http://localhost:8001
# ML 서버가 작업 결과를 콜백할 Gateway 주소
# 로컬 개발: http://localhost:3000
# 프로덕션 (docker-compose 내부 네트워크): http://gateway:3000
GATEWAY_PUBLIC_URL=http://localhost:3000
# ML 서버 → Gateway 웹훅 인증용 공유 시크릿 (ml-server .env 의 ML_WEBHOOK_SECRET 과 동일)
ML_WEBHOOK_SECRET=

# Community Service (event target)
COMMUNITY_SVC_HOST=localhost
COMMUNITY_SVC_PORT=3005

# Environment
NODE_ENV=development
