{
  "name": "tone-knob",
  "private": true,
  "workspaces": [
    "front-end",
    "back-end",
    "real-time"
  ],
  "scripts": {
    "dev:front": "npm run dev --workspace front-end",
    "dev:back": "npm run start:dev --workspace back-end",
    "dev:rt": "npm run dev --workspace real-time",
    "build": "npm -ws run build",
    "lint": "npm -ws run lint",
    "test": "npm -ws run test"
  }
}
