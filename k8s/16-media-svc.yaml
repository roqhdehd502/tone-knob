apiVersion: apps/v1
kind: Deployment
metadata:
  name: media-svc
  namespace: tone-knob
spec:
  replicas: 2
  selector:
    matchLabels:
      app: media-svc
  template:
    metadata:
      labels:
        app: media-svc
    spec:
      containers:
        - name: media-svc
          image: tone-knob/media-svc:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: tone-knob-config
            - secretRef:
                name: tone-knob-secrets
          ports:
            - containerPort: 3008
          readinessProbe:
            tcpSocket:
              port: 3008
            initialDelaySeconds: 5
          livenessProbe:
            tcpSocket:
              port: 3008
            initialDelaySeconds: 15
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: media-svc
  namespace: tone-knob
spec:
  selector:
    app: media-svc
  ports:
    - port: 3008
      targetPort: 3008
