apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-svc
  namespace: tone-knob
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ai-svc
  template:
    metadata:
      labels:
        app: ai-svc
    spec:
      containers:
        - name: ai-svc
          image: tone-knob/ai-svc:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: tone-knob-config
            - secretRef:
                name: tone-knob-secrets
          ports:
            - containerPort: 3009
          readinessProbe:
            tcpSocket:
              port: 3009
            initialDelaySeconds: 5
          livenessProbe:
            tcpSocket:
              port: 3009
            initialDelaySeconds: 15
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: ai-svc
  namespace: tone-knob
spec:
  selector:
    app: ai-svc
  ports:
    - port: 3009
      targetPort: 3009
